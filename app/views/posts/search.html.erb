<p id="notice"><%= notice %></p>

<h1>Posts</h1>
<%= form_tag search_posts_path, method: :get, class: "navbar-form navbar-right", role: "search" do |f|%>
  <p>
    <%= check_box_tag :search, "낮부스" %>
    <%= label_tag :search, "낮부스" %>
    <%= check_box_tag :search, "밤주점" %>
    <%= label_tag :search, "밤주점" %>
    <%= submit_tag "Search", name: nil, class: "btn btn-default" %>
  </p>
<% end %>
<%= form_tag search_posts_path, method: :get, class: "navbar-form navbar-right", role: "search" do |f|%>
  <p>
    <%= text_field_tag :search , nil, valud: ""%>
    <%= submit_tag "Search", name: nil, class: "btn btn-default" %>
  </p>
<% end %>
<table>
  <thead>
    <tr>
      <th>Local</th>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
<h2 class="text-center">부스 정보</h2>

<div class="container pt-3">
  <div class="row">
    <% @posts.each do |post| %>
      <div class="py-2 col-8 col-sm-4 col-md-3 col-lg-2 mx-auto">
        <div class="card text-center">
          <%= image_tag post.menuimg.url, class: "card-img-top" %>
          <div class="card-block">
            <h4 class="card-title font-weight-bold"><%=post.name%></h4>
            <%= link_to '보러가기', post ,class: "btn btn-secondary"%>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
